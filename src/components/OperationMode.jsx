import React from 'react';
import { Typography, Box } from '@mui/material';


const OperationMode = () => {
  return (
    <Box>
      <Typography variant="body1">Here you can display or modify the current Operation Mode.</Typography>
      {/* Add dropdown, toggles, or display logic here */}
    </Box>
  );
};

export default OperationMode;
